# 添加类支持

1. 添加类的bnf范式解析支持
2. 给成员添加添加self参数
3. 在compile code chunk里，对类节点编译
4. 添加类成员函数的符号解析
5. 添加new支持


基本解决完了，遇到的问题：

## new

本来是想给new在生成ast期间就创建对应的节点，但是实际这样应付不了大多数编码环境，所以还是以点语法的形式存在，给Function派生了子类，在eval时多判断下是不是NewOperator。

## 函数调用

对象的函数调用，判断出来是一个函数调用之后，再拿着这个节点找函数名的子节点（即对象节点），然后看它是不是class，如果是将对象添加到参数列表里

## Value 的 ValueRef 类型

代表的意义基本是可修改的左值，为实例的成员和变量表的值对象服务，使这些对象本身可以修改。所以这些对象基本上只能在factor这层存在，比如说，到了multiexpr这层的eval的时候，它不可能再是ref值类型。
所以在编码的时候需要注意判断。