# 定位变量

## 目的

程序中的每一个变量都对应着一个存储位置，一般是一个表结构（线性表或者哈希表），以函数内的局部变量为例，进入函数时，会创建一个对应的表，局部变量对应着这个表里的一个元素。

### 例外情况

有些在函数中使用的变量并不是局部变量，还有可能是函数内可访问的外部变量，或者成员变量，由于变量重名经常会导致代码出bug，所以我们现在这里规定，全部变量/成员变量/局部变量不可有重名情况，实际一般语言都是允许的。

### 优先级

我们需要确定一下访问优先级

先查找全局表里有没有这个变量，如果没有，再从成员变量里查找有没有这个变量，如果还没有，就认定它是局部变量。

* 全局
* 数据成员
* 局部变量