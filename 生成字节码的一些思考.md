# 生成字节码的一些思考 

开始，我对字节码的理解为是完全像汇编那样的指定，但后来我想了想，指令未必需要像汇编一样，而尽量是能完成复杂逻辑的，可组合的指令。
为什么要是有一定的复杂度呢？
回想我们平时用的那些指定，mov啦，加减乘除运算啦，循环啦，确实是非常简单，不过是因为CPU确实有这种需求，更通用，更易实现，更简洁。但是，你们有没有发现，后来CPU出了更多的SIMD指令，
他们图什么呀！！！！！！！
其实就是让一个指令多做些事，效率更快（执行指令的次数会减少，放虚拟机里就能少走更多逻辑），这样是跟据实际情况一个取舍的过程。
到这里，我们是脚本，可以说是更高层次的一种抽象了，二元操作对象不再是具体几个字节，数据基本单元是object!!!!所以我们的指定也应该相关调整成与object相关的指令！！！！更进一步，我们可以也可以做一些集成更高又可复用的指令（特化指令），
比如
* dot/get field
* 调整函数栈
  
所以，我们的指令要能反映出我们vm的特征，vm天然支持的功能，围绕着这些功能设计指令！这样我们的vm才能跑得更快呀，绝对不能学术派，死脑筋。