# 脚本程序执行过程

1. token 分解
2. 生成ast
3. 创建包内符号表，生成模块信息（初始化）
3. 分析ast有无明显语法错误
4. 执行正确的ast，但实际上依然有执行错误，比如对某些类型执行不支持的操作


零碎：

全局变量不可以以astnode形式存在，因为在传递值的时候

模块重载

* 根包
  * 子包
    * 模块
    * 模块

同包内模块共享访问范围

* Env
  * 模块mapping - 