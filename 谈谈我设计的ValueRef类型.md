# PrimeVType::ValueRef

设计这个类型的初衷是为了拿到某个变量的引用，当我们给这个引用赋值的时候，就等价于给被引用的对象赋值了。
比如：

```script
void entry() {
    var a = 0
    a = 2
}
```
执行a = 2，生成机器码的执行过程，将a的引用压栈，给a的引用赋值为2，弹栈。
也许你觉得很没必要，实际上还有这种情况
```script
void entry() {
    var vec = [1,2,3,4]
    vec[1] = 4
}
```
我们将vec[1]的引用压栈，这样就能修改了，否则，如果没有引用就不容易处理这个过程。

但，同时它也带来一个问题，比如，我之前在栈上压了一个ValueRef值，栈弹回，下次再压的时候，这个位置已经是个ValueRef的值了，给一个ValueRef的值赋值，会导致逻辑错误，所以我们需要注意一个原则，那就是，栈顶之前的所有Value，都不能是ValueRef类型，不然会导致逻辑错误，所以我们在弹栈的时候需要让sp上的值全不为ValueRef，需要有这么一个流程！