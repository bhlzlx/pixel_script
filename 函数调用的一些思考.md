# 函数调用的一些思考

global_func(e,f,g)  
a.b.c.d(e,f,g)，现在匹配括号部分了，看上级，也就是c.d  
实际上，这个全局的函数调用看似没有点语法，实际是有一个隐匿的全局包/this对象，但是这些对象语法上不要求显式调用，这样就需要我们在后期再处理，引入很多麻烦，所以你们看为什么lua使用 `obj:fun()` 这种，在语法上是非常取巧的方式，这样就强制你写`self:member_func()`，省去了很多步骤，是非常偷懒方便的做法。

利用bnf范式，点语言去访问其实是


## 压栈与传参

当我们调用`obj.func(expr1, expr2)`的时候，先设置fp作为栈底标记，还是先计算参数表达式？答案是要先计算参数表达式，将结果压栈，因为表达式里的符号上下文环境，作用哉是当前作用域内的，但是新一栈帧fp依然需要是参数压栈前的，所以需要一点微小的处理，才能使之正常。

目前的函数调用方式是这样的

先压参数，再压self或者空，再压函数字段名/bytecodeFunction，如果有self则用字段名去取，如果没有self，则直接压bytecodefunction.预调用函数的时候取得函数对象，弹出，取得self，设置self寄存器，弹栈。设定fp,ap,lp,sp。搞定！


## 如何支持不定参

虽然我现在没支持不定参，但是实现方法我早就想好了，在frame上加个不定参存储就好了，easy easy，不过我们现在完全不需要，感觉没啥用。
虽然说不支持不定参，但是实际上对bridge function是支持不定参的。

